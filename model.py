import random

import tensorflow as tf
import tensorflow.keras.backend as K

from tensorflow.keras.layers import (
    Dense, 
    Permute, 
    Softmax, 
    Activation, 
    Add,
    Layer,
    LSTM
)

# The code will be written when the testing of test_model_seprated_class and test_model_merged_class is complete.
